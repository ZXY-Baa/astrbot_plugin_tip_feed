{
  "trigger_keywords": {
    "description": "触发关键词",
    "type": "string",
    "default": "投喂",
    "hint": "多个关键词用英文逗号分隔，例如：投喂,打赏,赞助"
  },
  "qrcode_path": {
    "description": "收款码图片路径",
    "type": "string",
    "default": "",
    "hint": "支持本地文件路径（如 /path/to/qrcode.png）或图片 URL（以 http/https 开头）"
  }
}
